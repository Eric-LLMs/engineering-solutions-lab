# 功能回归
---

- name: regression
  hosts: "{{ (groups[ 'live_comments_analysis_' + cluster ] | shuffle)[0] }}"
  tasks:
    # 回归
    - name: regression
      include_role:
        name: common-service/regression
      vars:
        ansible_port: "{{ ssh_port }}"
        ansible_ssh_pass: "{{ docker_root_passwd }}"
