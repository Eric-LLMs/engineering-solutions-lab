Global:
  tool: build_submitter
Default:
  profile: [sid_tfidf.vec]
Profiles:
  - profile:
  name: sid_tfidf.vec
  env: centos4u3
  command: mkdir output && cp ci.yml output/ && echo hello ci-build
  release: true
